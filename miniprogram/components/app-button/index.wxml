<button class="app-button app-button--{{variant}} {{loading || disabled ? 'app-button--disabled' : 'tap-scale'}}" loading="{{loading}}" disabled="{{loading || disabled}}" bindtap="handleTap">
  <slot></slot>
</button>
