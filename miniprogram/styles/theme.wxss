.hairline-top {
  position: relative;
}

.hairline-top::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: rgba(38, 50, 56, 0.08);
  transform: scaleY(0.5);
}

.safe-bottom {
  padding-bottom: env(safe-area-inset-bottom);
}
